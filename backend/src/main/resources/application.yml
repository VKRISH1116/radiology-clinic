spring:
  application:
    name: radiology-clinic-backend

  # Report uploads are PDFs; cap the multipart size (service also re-checks 20MB).
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB

  # Connects to the PostgreSQL started by docker-compose.yml.
  # Values fall back to the dev defaults but can be overridden by env vars.
  datasource:
    # Priority: an explicit DB_URL (docker-compose sets one) wins; otherwise the
    # URL is built from parts. Managed hosts (e.g. Render Postgres) expose host/
    # port/name/user/password separately, so we assemble the JDBC URL from them.
    # With nothing set, it falls back to the local dev database.
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:radiology_clinic}}
    username: ${DB_USER:clinic}
    password: ${DB_PASSWORD:clinic_dev_pw}

  jpa:
    hibernate:
      ddl-auto: validate   # Flyway OWNS the schema; Hibernate only validates entities against it
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration   # V1__, V2__ ... run in order on startup

server:
  # PaaS platforms inject the port to listen on via PORT; default to 8080 locally.
  port: ${PORT:8080}

# App-specific settings (read by JwtService via @Value).
app:
  jwt:
    # HS256 signing key. MUST be >= 32 bytes. Dev default is fine locally;
    # in real deployments set the JWT_SECRET env var to a random secret.
    secret: ${JWT_SECRET:dev-secret-please-change-me-this-must-be-at-least-32-bytes-long}
    # Access-token lifetime in ms (15 min). Short-lived on purpose: clients use
    # the refresh token to get new ones, so a leaked access token expires soon.
    expiration-ms: ${JWT_EXPIRATION_MS:900000}
    # Refresh-token lifetime in ms (30 days).
    refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:2592000000}
  # Initial admin, created at startup if absent. CHANGE THESE for real deploys.
  bootstrap-admin:
    email: ${BOOTSTRAP_ADMIN_EMAIL:admin@clinic.local}
    password: ${BOOTSTRAP_ADMIN_PASSWORD:admin-dev-password}
  booking:
    # Patients may self-cancel/reschedule only up to this many hours before the slot.
    change-cutoff-hours: ${BOOKING_CHANGE_CUTOFF_HOURS:2}
  consent:
    # The consent/privacy terms version captured when a patient profile is created.
    current-version: ${CONSENT_VERSION:v1}
  reports:
    # Where uploaded report PDFs are stored (kept OUTSIDE any web root).
    dir: ${REPORTS_DIR:./data/reports}
  cors:
    # Browser origins allowed to call the API (comma-separated). The Vite dev
    # server runs on 5173; add the deployed frontend origin for prod.
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
